'use client'

import { useActionState } from 'react'
import { saveSettings, restoreBackup } from '@/actions/settings'

type Settings = { siteTitle: string; siteDescription: string; socialImageUrl: string | null; themeMode: string; primaryColor: string; accentColor: string; animations: boolean; backgroundEffects: boolean; seoEnabled: boolean }

export default function SettingsForm({ settings }: { settings: Settings }) {
  const [restoreState, restoreAction, restoring] = useActionState(restoreBackup, null)

  return <div className="space-y-6"><form action={saveSettings} className="grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-6"><h2 className="text-xl font-semibold">SEO manager</h2><div className="mt-5 space-y-4"><label className="block text-sm text-slate-300">Page title<input name="siteTitle" defaultValue={settings.siteTitle} className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label><label className="block text-sm text-slate-300">Description<textarea name="siteDescription" defaultValue={settings.siteDescription} rows={4} className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label><label className="block text-sm text-slate-300">Social preview image URL<input name="socialImageUrl" defaultValue={settings.socialImageUrl || ''} className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label><label className="flex items-center gap-2 text-sm text-slate-300"><input name="seoEnabled" type="checkbox" defaultChecked={settings.seoEnabled} /> Enable SEO metadata</label></div></section><section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-6"><h2 className="text-xl font-semibold">Theme customizer</h2><div className="mt-5 space-y-4"><label className="block text-sm text-slate-300">Mode<select name="themeMode" defaultValue={settings.themeMode} className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"><option value="dark">Dark</option><option value="light">Light</option></select></label><label className="block text-sm text-slate-300">Primary color<input name="primaryColor" type="color" defaultValue={settings.primaryColor} className="mt-2 block h-10 w-full rounded-lg border border-slate-700 bg-slate-950" /></label><label className="block text-sm text-slate-300">Accent color<input name="accentColor" type="color" defaultValue={settings.accentColor} className="mt-2 block h-10 w-full rounded-lg border border-slate-700 bg-slate-950" /></label><label className="flex items-center gap-2 text-sm text-slate-300"><input name="animations" type="checkbox" defaultChecked={settings.animations} /> Enable animations</label><label className="flex items-center gap-2 text-sm text-slate-300"><input name="backgroundEffects" type="checkbox" defaultChecked={settings.backgroundEffects} /> Enable background effects</label></div></section><button className="rounded-lg bg-cyan-400 px-4 py-2 font-semibold text-slate-950 xl:col-span-2">Save settings</button></form><section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-semibold">Backup & restore</h2><p className="mt-1 text-sm text-slate-400">Export all portfolio data or verify a JSON snapshot for later restoration.</p></div><a href="/api/backup" className="rounded-lg border border-slate-700 px-4 py-2 text-sm text-slate-200">Download backup</a></div><form action={restoreAction} className="mt-5"><textarea name="backup" rows={5} placeholder="Paste backup JSON here" className="w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-sm" /><button disabled={restoring} className="mt-3 rounded-lg border border-cyan-400 px-4 py-2 text-sm text-cyan-300">{restoring ? 'Checking...' : 'Verify and store snapshot'}</button>{restoreState?.error && <p className="mt-3 text-sm text-red-400">{restoreState.error}</p>}{restoreState?.success && <p className="mt-3 text-sm text-emerald-400">{restoreState.success}</p>}</form></section></div>
}
